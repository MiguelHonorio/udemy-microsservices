spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_estudo?useTimezone=true&serverTimezone=UTC
    username: root
    password: Admin
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#      dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
